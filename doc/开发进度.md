## 2026-04-11

- 使用 Gradle Wrapper 初始化 `project/server` 后端工程，完成 Spring Boot 基础结构搭建
- 配置 PostgreSQL 数据源，连接 `192.168.31.221:5432/wd`
- 完成基础后端接口与算法服务调用骨架，提供系统健康检查与概览接口
- 执行 `./gradlew clean build` 验证通过，并完成本地启动与接口联调
- 初始化 `project/fe` 前端工程，接入 Vue3、Naive UI、Vue Router、Pinia、ECharts
- 完成登录页、首页驾驶舱、数据管理、训练任务、预测分析页面首版
- 配置 Vite 代理与远程访问地址，执行 `npm run build` 验证通过
- 完成大屏页与三维场景页的后端接口联调，接入真实预测摘要、任务状态、告警与场景设备数据
- 升级三维页为 Three.js 场景表现，补齐前端类型依赖并修复历史数据空值兼容问题
- 执行 `./gradlew test` 与 `npm run build` 验证通过，接口已完成本地联调
- 完成右下角 AI 智能助手接入，支持多轮对话、图片发送、新建会话、清空记录与细腻开合动画
- 后端接入 SiliconFlow 的 Qwen/Qwen3.5-4B，并补充代理访问与 reasoning 返回兼容处理
- 清理 AI 助手删除后的前后端残留引用，移除全局挂载与类型声明，重新执行 `./gradlew test`、`npm run build` 验证通过
- 调整 AI 助手为仅展示最终回答，移除截图按钮，并补充流式中断兜底处理；再次执行 `./gradlew test`、`npm run build` 验证通过

## 2026-04-30

- 将后端配置文件从 `application.properties` 改为 `application.yml`
- 创建数据库表 `flow_raw_data`、`flow_dataset`、`flow_dataset_record` 及索引
- 新增 `FlowRawData`、`FlowDataset` JPA 实体与对应的 Repository
- 实现 `FlowDataService`：支持 CSV 多特征数据导入（兼容多时间格式）、分页条件查询、单条编辑、批量删除、质量检测统计
- 实现 `DatasetService`：支持按时间范围创建训练数据集、自动计算质量评分与记录数
- 新增 `DataController`（`/api/data/*`）与 `DatasetController`（`/api/datasets/*`）REST API
- 前端新增 `api/data.ts`、`api/dataset.ts` 封装所有后端接口
- 重写 `DataManagementView.vue`：对接后端真实数据，实现文件上传弹窗、分页查询、批量删除、单条编辑、质量检测、数据集创建
- 数据管理页新增可视化图表：数据质量饼图、来源分布饼图、流量趋势折线图
- 后端 `./gradlew bootJar` 编译通过，API 联调测试通过（上传、查询、质量检测、数据集创建）
- 按 Java 规范重构后端：`FlowRawData`/`FlowDataset` 补充 `equals`/`hashCode`；新增 `FlowRawDataDto`、`FlowDatasetDto` 与 `DataMapper`，避免直接暴露 Entity；`DataController` 增加分页参数边界校验；`DatasetController` 增加时间格式解析与业务规则校验；`GlobalExceptionHandler` 补充 `IllegalArgumentException` 处理

## 2026-05-07

- 创建 `users` 用户表（V2__init_users_table.sql），含用户名、密码哈希、显示名、角色、启用状态字段
- 新增 `User` JPA 实体与 `UserRepository`
- 实现 `AuthService`：SHA-256 密码哈希校验、用户状态检查
- 新增 `AuthController`（`POST /api/auth/login`）登录接口，返回用户信息
- 前端新增 `api/auth.ts` 封装登录 API 调用
- 新增 Pinia `authStore`：管理登录状态、localStorage 持久化、自动登出
- 更新 `LoginView.vue`：对接真实登录接口，表单校验，回车快捷登录
- 更新 `router/index.ts`：添加全局路由守卫，未登录跳转登录页，已登录跳转仪表盘
- 更新 `AppLayout.vue`：显示当前登录用户名，退出时清除认证状态
- 默认管理员账号：admin / admin123
- 后端 `./gradlew compileJava` 与前端 `vue-tsc -b` 编译通过
- AppLayout 右上角新增用户信息区域：头像、用户名、角色，点击弹出下拉菜单含退出登录功能
